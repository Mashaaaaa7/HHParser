name: parser

services:

  api:
    build: ./backend
    ports:
      - "1337:1337"
    depends_on:
      - psql
    volumes:
      - ./backend/run:/home/app/run

  psql:
    image: postgres:latest

    ports:
      - 5432:5432

    environment:
      POSTGRES_PASSWORD: "postgres"
